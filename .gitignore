node_modules/

# Build output
dist/

# Playwright: reports, traces, and the throwaway database the e2e stack seeds itself
test-results/
playwright-report/
e2e/.tmp/

# SQLite's WAL-mode sidecars for the tracked packages/backend/signi.db — runtime
# state, written and removed as connections come and go
*.db-shm
*.db-wal
