{
  "name": "signi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=packages/backend\" \"npm run dev --workspace=packages/frontend\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/engine && npm run build --workspace=packages/phrase && npm run build --workspace=packages/backend && npm run build --workspace=packages/frontend",
    "start": "node packages/backend/dist/index.js",
    "seed": "npm run seed --workspace=packages/backend",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "phrases:random": "tsx packages/engine/test/tools/randomPhrase.ts",
    "typecheck": "tsc --noEmit -p packages/engine/tsconfig.test.json && tsc --noEmit -p packages/phrase/tsconfig.test.json && tsc --noEmit -p packages/frontend/tsconfig.test.json && tsc --noEmit -p packages/backend/tsconfig.test.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "concurrently": "^8.2.2",
    "jsdom": "^29.1.1",
    "typescript": "^5.4.5",
    "vitest": "^4.1.10"
  }
}
